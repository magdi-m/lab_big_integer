name: Build & Test

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, ma   uses: actions/checkout@v4

      - name: Configure
        run: cmake -B build -DCMAKE_BUILD_TYPE=Debug

      - name: Build
        run: cmake --build build -j$(nproc)

      - name: Run tests
     
